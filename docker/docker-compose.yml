version: "3.9"

services:
  web:
    image: nginx:alpine
    ports:
      - "3000:80"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost"]
      interval: 2s
      timeout: 1s
      retries: 30